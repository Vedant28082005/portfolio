
  # Update profile links and image

  This is a code bundle for Update profile links and image. The original project is available at https://www.figma.com/design/u7K6X9xTyxkPYKMw6coR4C/Update-profile-links-and-image.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  